#include <bits/stdc++.h>
using namespace std;

using ll = long long;
using ld = long double;

#define vt vector
#define pb push_back

int main() {
    cin.tie(nullptr)->sync_with_stdio(0);
    freopen("data.in", "r", stdin);

    int n, m; 
    cin >> n >> m;

    string s;
    cin >> s;

    for (int i = 0; i < m; i++) {
        char c = s[i];
        if 
    }

    return 0;
}